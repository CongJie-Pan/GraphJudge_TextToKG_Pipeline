# OpenAI API Configuration
# Copy this content to a .env file and fill in your actual API credentials

# Required: Your OpenAI API key
OPENAI_API_KEY=your_openai_api_key_here

# Optional: Custom API base URL (defaults to https://api.openai.com/v1)
# Uncomment and modify if using a different endpoint
# OPENAI_API_BASE=https://api.openai.com/v1

# Instructions:
# 1. Create a .env file in the same directory
# 2. Copy this content to the .env file
# 3. Replace 'your_openai_api_key_here' with your actual OpenAI API key
# 4. Optionally set OPENAI_API_BASE if using a custom endpoint
# 5. Make sure .env is added to your .gitignore file to keep credentials secure 